import React from 'react'
import { render } from 'react-dom'

import './index.css'

const rootComponent = <h1>Let's plan your saving goal</h1>
const rootElement = document.getElementById('app')

render(rootComponent, rootElement)
